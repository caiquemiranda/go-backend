# Configurações do servidor
PORT=8080

# Configurações de JWT
JWT_SECRET=sua-chave-secreta-aqui
JWT_EXPIRATION=24h

# Configurações de banco de dados (para implementação futura)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=blog
DB_SSL_MODE=disable 